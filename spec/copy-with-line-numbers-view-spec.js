'use babel';

import CopyWithLineNumbersView from '../lib/copy-with-line-numbers-view';

describe('CopyWithLineNumbersView', () => {
  it('has one valid test', () => {
    expect('life').toBe('easy');
  });
});
